## Core — required to run the API, pipeline, and tests in MOCK_MODE
## (no GPU / inference server needed). This is everything you need for
## local dev.
fastapi==0.139.0
uvicorn[standard]==0.50.2
pydantic==2.13.4
pydantic-settings==2.14.2
sqlite-vec==0.1.9
huey==3.1.1
pypdf==6.14.2
pymupdf==1.28.0
jinja2==3.1.6
pyyaml==6.0.3
openai==2.45.0
python-multipart==0.0.32
lingua-language-detector==2.2.0

## Test tooling
pytest==9.1.1

## --------------------------------------------------------------------
## Production model-serving path (MOCK_MODE=False)
## --------------------------------------------------------------------
## Nothing extra to `pip install` for OCR/embeddings/generation themselves
## — those are served by whatever OpenAI-compatible inference server(s)
## you point each role at (Ollama, vLLM, TEI, OpenAI itself, ...) over
## HTTP, via the `openai` package above. See README.md "Going from mock
## to real models" and .env.example for the three independent
## OCR_*/GENERATION_*/EMBEDDING_* settings.
